# native-tools / Graphify Gate

Native-tools application:

- Content architecture is file-backed and graph-friendly: `content/case-studies`, `content/writing`, `public/artifacts`, and `docs/pack-gates`.
- Each flagship case study includes a compact three-node architecture diagram model in frontmatter.
- Graphify is appropriate after dependencies are installed to generate a topology over this repo.

Deferred optional command:

```bash
graphify . --no-viz
```
